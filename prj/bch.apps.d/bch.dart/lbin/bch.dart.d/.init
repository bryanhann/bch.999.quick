#!/usr/local/bin
name=$(basename $real)
repo=$real
while [ ! -d $repo/.git ]; do
    repo=$(dirname $repo)
done
prj=$repo/prj
app=$(dirname $(dirname $real))/$name
upper=$(echo $name | cut -d'.' -f2 | tr '[:lower:]' '[:upper:]')

